<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Overlay-Berechtigung (Zeichnen über anderen Apps) -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    
    <!-- Berechtigungen für den Vordergrunddienst (ab Android 14 für die Bildschirmaufnahme nötig) -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PROJECTION" />

    <application
        android:allowBackup="true"
        android:icon="@android:drawable/sym_def_app_icon"
        android:label="DuLo"
        android:roundIcon="@android:drawable/sym_def_app_icon"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.DayNight.NoActionBar">

        <!-- Startbildschirm mit dem Umschalter -->
        <activity
            android:name=".ui.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
          Auto-Zug: Nur über einen Bedienungshilfen-Dienst darf eine App Berührungen an eine
          fremde App schicken. Der Nutzer muss ihn einmalig in den Systemeinstellungen freigeben;
          ohne Freigabe bleibt der Schalter wirkungslos.
        -->
        <service
            android:name=".service.DuloAutoMoveService"
            android:exported="false"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/dulo_autozug_dienst" />
        </service>

        <!-- Vordergrunddienst für Blase und Bildschirmanalyse (Typ mediaProjection ab Android 14) -->
        <service
            android:name=".service.FloatingBubbleService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="mediaProjection" />

    </application>

</manifest>
